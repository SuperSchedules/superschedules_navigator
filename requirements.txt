# Django
django>=5.0
psycopg2-binary>=2.9
gunicorn>=21.0
python-dotenv>=1.0

# Discovery dependencies
requests>=2.31.0
beautifulsoup4>=4.12.2
playwright>=1.40.0
httpx>=0.27.0

# OSM data extraction (streaming, low memory)
osmium>=3.7.0

# Reverse geocoding (local, no API calls)
reverse_geocoder>=1.5

# DuckDuckGo search (renamed from duckduckgo-search)
ddgs>=7.0

# Testing
pytest>=7.4.3
pytest-django>=4.5.0

# CLI/TUI (for future terminal app)
rich>=13.0
click>=8.0
